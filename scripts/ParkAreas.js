// Import the ParkAreas getter function from `database.js`

// Define and export a function to generate html for the ParkArea resources in the `database` object

// The function will have no parameters, it will call the ParkAreas getter function and
// store the returned copy of the array of ParkArea objects in a new variable,
// then map through that array, generating html for each ParkArea instance and join it into a single string of hmtl.
// The function will return the string of html. 